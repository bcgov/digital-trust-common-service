apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "digital-trust-common-service.fullname" . }}-test-connection"
  labels:
    {{- include "digital-trust-common-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.36.1
      args:
        - '--spider'
        - '-S'
        - 'http://{{ include "digital-trust-common-service.fullname" . }}:{{ .Values.service.port }}/health/live'
