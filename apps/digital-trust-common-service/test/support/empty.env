# Intentionally empty. trace-log-correlation.e2e-spec.ts points the probe's
# DOTENV_CONFIG_PATH here so that `import 'dotenv/config'` in tracing.ts finds
# nothing, and a developer's own .env cannot reach the SDK under test.
